
<tr>
    <td><%= employee.name %></td>
    <td><%= employee.address %></td>
    <td><%= employee.number %></td>
    <td><%= employee.rate_of_pay %></td>
    <td><% if current_user?(employee.user) %>
          <%= link_to '<span style="color: #DC003B;" class="glyphicon glyphicon-remove" aria-hidden="true"></span> DELETE'.html_safe, employee, method: :delete,
                                           data: { confirm: "You sure?" } %>

     <%end%>  </td>
    <td><% if current_user?(employee.user) %>
      <%= link_to('<span style="color: #6501FF;" class="glyphicon glyphicon-pencil" aria-hidden="true"></span> EDIT'.html_safe, edit_employee_path(employee.id)) %>

        <% end %></td>
 
  </tr>



  