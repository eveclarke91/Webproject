<h1>All Employee Holidays</h1>

<% if @holidays.empty? %>
<div class="alert alert-warning" role="alert">
  <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
  <span class="sr-only">Error:</span>
  No records found
</div>

<% else %>
<table class="table table-striped table-bordered">
	<thead>
  <tr>
    <th>Employee Name</th>
    <th>Start Date</th>
    <th>Finish Date</th>
    <th></th>
    <th></th>
</tr>
  </thead>
<tbody>

 <%= render @holidays %>

</tbody>

</table>

<br />

<%= link_to " Create a Holiday", "/holidays" %></li>
<% end %>